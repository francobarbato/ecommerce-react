


const ItemList = ({items}) => {
    return (
        <ProductsContainer>
            {
                // items.length > 0
                // ? items.map(item => <Item key={item.id} title={item.name} price={item.cost}/>)
                // : <p>Cargando<p/>
            }
        </ProductsContainer>
    );
}